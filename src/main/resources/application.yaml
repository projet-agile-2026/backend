spring:
  application:
    name: backend

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
    username: ${SPRING_DATASOURCE_USERNAME:DOSI}
    password: ${SPRING_DATASOURCE_PASSWORD:dosi}
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:none}
    show-sql: ${SPRING_JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.OracleDialect

server:
  port: 8083
  servlet:
    context-path: /

#
#spring:
#  datasource:
#    url: jdbc:oracle:thin:@//oracle21c_PA:1521/XEPDB1
#    username: DOSI
#    password: dosi
#    driver-class-name: oracle.jdbc.OracleDriver
#
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
#    database-platform: org.hibernate.dialect.OracleDialect
#
#  sql:
#    init:
#      mode: never
#
#server:
#  port: 8080